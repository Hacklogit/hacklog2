<?php
if (isset($_GET['signature']) {
	$signature_user = $_GET['signature'];

	// Qui una query che salva il valore all'interno di un database

}

// Qui una query che recupera la firma all'interno della variabile $row

echo $row["signature"];
?>